FROM python:3.11-slim

WORKDIR /app

# Копируем только один файл - без зависимостей
COPY railway_health.py .

# Делаем файл исполняемым
RUN chmod +x railway_health.py

EXPOSE 8080

# Запускаем напрямую Python без дополнительных зависимостей
CMD ["python3", "railway_health.py"]
